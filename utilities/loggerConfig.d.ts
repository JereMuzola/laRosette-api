export declare class LoggerConfig {
    private readonly options;
    private dirPathRoot;
    private dirPathDebug;
    private dirPathError;
    private dirPathInfo;
    constructor();
    init(): void;
    start(): object;
}
